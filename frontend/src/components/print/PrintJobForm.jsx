export default function PrintJobForm({ value = {}, onChange, onSubmit, isSaving }) {
  const set = (key, next) => onChange?.({ ...value, [key]: next });
  return <form onSubmit={(event) => { event.preventDefault(); onSubmit?.(); }} className="space-y-4"><input required className="w-full rounded-md border px-3 py-2" placeholder="Print job title" value={value.title || ''} onChange={(e) => set('title', e.target.value)} /><div className="grid gap-3 sm:grid-cols-3"><input required type="number" min="1" className="rounded-md border px-3 py-2" placeholder="Quantity" value={value.quantity || ''} onChange={(e) => set('quantity', Number(e.target.value))} /><input required type="date" className="rounded-md border px-3 py-2" value={value.requiredDate || ''} onChange={(e) => set('requiredDate', e.target.value)} /><select className="rounded-md border px-3 py-2" value={value.printJobType || 'other'} onChange={(e) => set('printJobType', e.target.value)}><option value="catalogue">Catalogue</option><option value="brochure">Brochure</option><option value="flyer">Flyer</option><option value="banner">Banner</option><option value="other">Other</option></select></div><textarea className="w-full rounded-md border px-3 py-2" placeholder="Description" value={value.description || ''} onChange={(e) => set('description', e.target.value)} /><button className="rounded-md bg-primary px-4 py-2 text-sm font-semibold text-white" disabled={isSaving}>{isSaving ? 'Saving...' : 'Save Print Job'}</button></form>;
}
